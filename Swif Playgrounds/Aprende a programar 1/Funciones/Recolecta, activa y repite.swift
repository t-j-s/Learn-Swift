/*
Reto: define una función para un patrón recursivo.

En este reto, debes recolectar las gemas que están junto a 
los interruptores.

En lugar de repetir el mismo patrón de comandos que usaste en los 
rompecabezas anteriores, puedes usar una nueva función que incluya 
los comandos existentes usados para cada par de gemas e interruptor.

Puedes darle el nombre que quieras a tu función en este reto. Una vez que le 
asignes un nombre a tu función, llámala. Para hacerlo, sólo ingresa su nobre, 
tal y como lo has hecho con las otras funciones que has usado.
*/
func go {
	moveForward()
	collectGem()
	moveForward()
	toggleSwitch()
	moveForward()
}
go()
turnLeft()
go()
moveForward()
turnLeft()
go
turnLeft()
go()